```plantuml
    @startuml UseCase
        actor UserGiver
        actor UserAcepter
        actor DiscordServer

        rectangle Bot{
            usecase GetTask
            usecase Polling
            usecase 
        }

    @endmul
```

